# JSProject5_Menu
The purpose of this project is to make a restaurant menu that displays info dynamically depending on the data provided by a database. 
This proyect is focused on javaScript. It filters the items categories and displays them in form of filter buttons so the user can filter the items. 
The items are also displayed dynamically by mapping every object from the database.


